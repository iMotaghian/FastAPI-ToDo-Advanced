# 1. env:
python -m venv venv
venv\Scripts\activate

# 2. installing requirements
pip install fastapi[all] alembic sqlalchemy
pip freeze > requirements.txt

# 3. setup alembic
cd core
alembic init alembic

# 4. main.py config.py
# 5. env
# 6. databese.py

# 7. update tasks.routers.py and use in main.py
from tasks.routes import router as tasks_routers
app.include_router(tasks_routers)